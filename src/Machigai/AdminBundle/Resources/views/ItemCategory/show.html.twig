{% extends "MachigaiAdminBundle::base.html.twig" %}

{% block contents -%}
    <h1>アイテムカテゴリ</h1>
	<a href="{{ path('item') }}">アイテム一覧に戻る</a><br />
	<a href="{{ path('itemcategory') }}">アイテムカテゴリ一覧に戻る</a>｜<a href="{{ path('itemcategory_edit', { 'categoryCode': entity.categoryCode }) }}">編集</a>

    <table class="table table-striped table-bordered table-hover compact">
        <tbody>
            <tr>
                <th width="20%">カテゴリコード</th>
                <td>{{ entity.categoryCode }}</td>
            </tr>
            <tr>
                <th>カテゴリ名</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

	{{ form(delete_form) }}
	
{% endblock %}
